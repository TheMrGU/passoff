{
  "name": "passoff",
  "version": "0.1.0",
  "description": "Context Passoff — structured handoffs between AI coding agents via MCP",
  "bin": { "passoff": "./dist/index.js" },
  "main": "./dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "latest",
    "better-sqlite3": "^12",
    "commander": "^12",
    "nanoid": "^5",
    "zod": "^3"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/better-sqlite3": "^7",
    "typescript": "^5",
    "vitest": "^2"
  },
  "engines": { "node": ">=20" },
  "license": "MIT"
}
