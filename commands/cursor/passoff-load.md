Load the most recent Passoff handoff for the current project.

1. Call the `passoff_load` MCP tool with `{ "latest": true }`.
2. Read the returned handoff carefully — especially **Current state**, **Next steps**, and **Gotchas**.
3. Summarize in 2–3 sentences who handed off and what the next step is.
4. Wait for the user to confirm before executing the next step.
